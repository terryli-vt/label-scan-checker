{
  "name": "label-scan-checker",
  "version": "1.0.0",
  "description": "A web app for verifying if scanned product labels match target strings for quality control.",
  "main": "index.js",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "start": "npm run server",
    "server": "cd server && nodemon app.js",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "proxy": "http://localhost:3500",
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}
